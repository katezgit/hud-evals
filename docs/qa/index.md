# QA

Quality assurance artifacts.

- **`e2e-bugs/`** — Bug reports written by E2E test agents when Playwright tests reveal app bugs. One markdown file per bug, named `<description>.md`. Tests never modify app code directly.
- **`screenshots/`** — Visual captures from Chrome DevTools for product-designer review. Used to surface UI/visual issues against design specs.
